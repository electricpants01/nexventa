---
import { Image } from 'astro:assets'
import imgLogo from '@images/cliente/Logo-KFC.png'

const logo = {
  name: 'KFC',
}
---

<a href="#" class="inline-block transition-transform transform hover:scale-110">
  <Image src={imgLogo} alt={logo.name} class="rounded-full" width={70} height={50} />
</a>
